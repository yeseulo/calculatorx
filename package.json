{
  "name": "calculatorx",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "private": true,
  "scripts": {
    "start": "parcel index.html --open",
    "watch": "parcel watch index.html",
    "build": "parcel build index.html --public-url /calculatorx/",
    "deploy": "gh-pages -d dist",
    "predeploy": "rm -rf dist/ && yarn run build"
  },
  "homepage": "https://yeseulo.github.io/calculatorx",
  "dependencies": {
    "gh-pages": "^3.1.0"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1",
    "sass": "^1.30.0"
  },
  "keywords": []
}
